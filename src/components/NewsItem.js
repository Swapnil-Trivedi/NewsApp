import React, { Component } from 'react'
import PropTypes from 'prop-types'

export class NewsItem extends Component {
  static propTypes = {

  }

  render() {
    return (
      <div>
        This is a news Item detail
      </div>
    )
  }
}

export default NewsItem
